# cd14452 ML Optimization C1 - venv pins (CONUPDATE-5029)
#
# These pins reproduce the shared venv used by every notebook in this course.
# Install into a fresh virtualenv (Python 3.10) with:
#
#   python3 -m venv --system-site-packages /path/to/cd14452-venv
#   /path/to/cd14452-venv/bin/pip install --upgrade pip
#   /path/to/cd14452-venv/bin/pip install -r requirements.txt
#
# On Vocareum the venv lives at /voc/data/cd14452-venv and is provisioned
# by the container's Configure Workspace step. Kernel is registered at
# /usr/local/share/jupyter/kernels/cd14452-venv.
#
# Torch/torchvision/triton must come from the PyTorch CUDA 12.1 index.

--extra-index-url https://download.pytorch.org/whl/cu121

torch==2.3.1
torchvision==0.18.1
triton==2.3.1

# --- Hugging Face core ---
transformers==4.45.2
tokenizers==0.20.3
accelerate==0.34.2
datasets==2.19.2
evaluate==0.4.2
peft==0.11.1
optimum==1.27.0

# --- Quantization / pruning stacks ---
bitsandbytes==0.44.1
optimum-intel==1.21.0
optimum-quanto==0.2.2
neural_compressor==3.7.1
onnx==1.22.0
onnxruntime==1.14.1

# --- Numerical / support ---
numpy==1.26.4
ml_dtypes==0.5.4
sympy==1.14.0
mpmath==1.3.0
networkx==3.4.2
pillow==12.3.0
schema==0.7.8

# --- CUDA runtimes (pulled by torch, pinned for reproducibility) ---
nvidia-cublas-cu12==12.1.3.1
nvidia-cuda-cupti-cu12==12.1.105
nvidia-cuda-nvrtc-cu12==12.1.105
nvidia-cuda-runtime-cu12==12.1.105
nvidia-cudnn-cu12==8.9.2.26
nvidia-cufft-cu12==11.0.2.54
nvidia-curand-cu12==10.3.2.106
nvidia-cusolver-cu12==11.4.5.107
nvidia-cusparse-cu12==12.1.0.106
nvidia-nccl-cu12==2.20.5
nvidia-nvjitlink-cu12==12.9.86
nvidia-nvtx-cu12==12.1.105

# --- Jupyter runtime ---
ipykernel==7.3.0
ipython==8.39.0
jupyter_client==8.9.1
jupyter_core==5.9.1
comm==0.2.3
debugpy==1.8.21
nest-asyncio2==1.7.2
tornado==6.5.8
pyzmq==27.1.0
traitlets==5.16.1
matplotlib-inline==0.2.2
jedi==0.20.0
parso==0.8.7
prompt_toolkit==3.0.53
Pygments==2.20.0
stack-data==0.6.3
asttokens==3.0.2
executing==2.2.1
pure_eval==0.2.3
decorator==5.3.1
wcwidth==0.8.2
pexpect==4.9.0
ptyprocess==0.7.0
platformdirs==4.11.3
psutil==7.2.2

# --- Data / IO ---
aiobotocore==2.26.0
aiohappyeyeballs==2.7.1
aiohttp==3.14.3
aiosignal==1.4.0
botocore==1.41.5
propcache==0.5.2
yarl==1.24.5
fsspec==2024.3.1
multiprocess==0.70.16
dill==0.3.8
pyarrow-hotfix==0.7
python-dateutil==2.9.0.post0

# --- Utility ---
Deprecated==1.3.1
Jinja2==3.1.6
MarkupSafe==3.0.3
coloredlogs==15.0.1
humanfriendly==10.0
exceptiongroup==1.3.1
filelock==3.32.3
packaging==26.3
ninja==1.13.0
six==1.17.0
typing_extensions==4.16.0

# ============================================================================
# System-baseline deps (NOT installed by this file — inherited from the
# container's system Python via --system-site-packages). The kernel runs with
# PYTHONNOUSERSITE=1, so these resolve from /usr/lib/python3/dist-packages, NOT
# from /voc/work/.local (which is per-container scratch and wiped on restart).
# Listed here only as a reference for the versions the notebooks were validated
# against; if a future image baseline drifts and the HF stack breaks, pin the
# offending package explicitly ABOVE this line to override the system version.
#   huggingface_hub 0.34.4   safetensors 0.4.1     regex 2023.3.23
#   sentencepiece 0.1.98     protobuf 6.32.0       tqdm 4.67.3
#   requests 2.32.5          pyarrow 15.0.2        scipy 1.15.3
#   pandas 2.3.3             scikit-learn 1.7.2    PyYAML 6.0.3
# ============================================================================
